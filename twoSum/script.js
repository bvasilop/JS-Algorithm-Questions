function twoSum(numArray, sum) {
    // Returns every pair of numbers from 'numArray' that adds up to the 'sum'

    // should return an array of filled with every pair of numbers from our 'numArray' that adds up to the 'sum'

    // eg. numArray = [ 1, 6, 4, 5, 3, 3 ] sum = 7
        // result = [ [ 6, 1 ], [ 3, 4 ], [ 3, 4] ]

    /******** Guidelines ********/
    // 1. Result should be an array of arrays
    // 2. Any number in the 'numArray' can be used in multiple pairs

        // Multiple ways of accomplishing result:
            // 1. Can be done in O (n^2) time complexity // involves nested loops
            // 2. Can be done in O (n) time complexity // more performing // involves using hash tables


}
